{
  "manifest_version": 3,
  "name": "rumcap capture (demo)",
  "version": "0.1.0",
  "description": "Demo harness: injects the rumcap capture demo into pages and adds the Document-Policy: js-profiling header so the JS Self-Profiler works, then saves a .rcap on page hide.",
  "minimum_chrome_version": "111",
  "permissions": ["declarativeNetRequest"],
  "host_permissions": ["<all_urls>"],
  "declarative_net_request": {
    "rule_resources": [{ "id": "rumcap_rules", "enabled": true, "path": "rules.json" }]
  },
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "run_at": "document_start",
      "world": "MAIN",
      "js": ["injected.js"]
    }
  ]
}
